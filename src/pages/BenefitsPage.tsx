import { Navigate } from 'react-router-dom'

/** Benefits folded into Membership per product IA — keep URL working. */
export default function BenefitsPage() {
  return <Navigate to="/membership" replace />
}
